import { notification } from "antd";
import { notifyTypes } from "../types/utilsTypes";

export const notify: notifyTypes = (type, title, msg, duration) => {
  // notification.config({ message: title, description: String(msg), duration: duration });
}